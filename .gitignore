# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Dependency directories (remove the comment below if you want to ignore them)
# vendor/

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Environment/local configs
.env
*.env.local
*.env.development.local
*.env.test.local
*.env.production.local

# Production TLS certificates (typically)
# Keep development certs for ease of setup, but exclude production ones
certs/production/
*.pem
# Uncomment the line below if you want to exclude all certificates
certs/

# Logs
*.log
logs/

# gRPC generated code
# The generated Go files are usually committed, but you can exclude them if preferred
# *.pb.go

# Build output
bin/
dist/
build/
tmp/

# Auth service specific
# Session data (if stored on disk)
sessions/
# User data (if saved locally for testing)
data/

questions.txt